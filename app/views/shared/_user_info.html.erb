<%= link_to gravatar_for(current_user, size: Settings.user_info.size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_pro"), current_user %></span>
<span><%= pluralize(current_user.microposts.size, t(".micropost")) %></span>
